import { MetadataConfiguration } from './metadata-configuration';

describe('MetadataConfiguration', () => {
  it('should create an instance', () => {
    expect(new MetadataConfiguration()).toBeTruthy();
  });
});
