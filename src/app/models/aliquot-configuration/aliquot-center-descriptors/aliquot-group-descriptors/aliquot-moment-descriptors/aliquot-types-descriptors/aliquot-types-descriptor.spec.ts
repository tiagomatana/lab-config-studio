import { AliquotTypesDescriptor } from './aliquot-types-descriptor';

describe('AliquotTypesDescriptor', () => {
  it('should create an instance', () => {
    expect(new AliquotTypesDescriptor()).toBeTruthy();
  });
});
