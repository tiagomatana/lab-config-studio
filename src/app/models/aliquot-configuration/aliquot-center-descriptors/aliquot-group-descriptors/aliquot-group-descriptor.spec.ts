import { AliquotGroupDescriptor } from './aliquot-group-descriptor';

describe('AliquotGroupDescriptor', () => {
  it('should create an instance', () => {
    expect(new AliquotGroupDescriptor()).toBeTruthy();
  });
});
