import { AliquotCenterDescriptor } from './aliquot-center-descriptor';

describe('AliquotCenterDescriptors', () => {
  it('should create an instance', () => {
    expect(new AliquotCenterDescriptor()).toBeTruthy();
  });
});
