import { AliquotConfiguration } from './aliquot-configuration';

describe('AliquotConfiguration', () => {
  it('should create an instance', () => {
    expect(new AliquotConfiguration()).toBeTruthy();
  });
});
