import { CollectGroupConfiguration } from './collect-group-configuration';

describe('CollectGroupDescriptor', () => {
  it('should create an instance', () => {
    expect(new CollectGroupConfiguration()).toBeTruthy();
  });
});
