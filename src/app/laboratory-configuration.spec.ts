import { LaboratoryConfiguration } from './laboratory-configuration';

describe('LaboratoryConfiguration', () => {
  it('should create an instance', () => {
    expect(new LaboratoryConfiguration()).toBeTruthy();
  });
});
